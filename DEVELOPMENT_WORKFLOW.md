# Development workflow

Active development happens on `dev`.
Stable, tested checkpoints are promoted to `main`.

Every milestone must include:
1. tests / Godot editor-load verification;
2. commit with a clear message;
3. push to `dev`;
4. update `CHECKPOINT.md`;
5. optionally tag/promote to `main` after a stable playtest.
